import{describe,it,expect}from"vitest";import{readFileSync}from"node:fs";const sql=readFileSync("supabase/migrations/202606040001_initial_schema.sql","utf8");describe("database security",()=>{it.each(["profiles","teachers","students","invitations"])("enables RLS for %s",table=>expect(sql).toContain(`alter table ${table} enable row level security`));it("uses authenticated identity when accepting",()=>expect(sql).toContain("where id=auth.uid()"));it("does not use service role keys",()=>expect(sql.toLowerCase()).not.toContain("service_role"))});
