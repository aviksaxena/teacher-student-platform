"use client";import{useState}from"react";export function Modal({trigger,title,children}:{trigger:string;title:string;children:React.ReactNode}){const[open,setOpen]=useState(false);return<><button className="btn" onClick={()=>setOpen(true)}>{trigger}</button>{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/60 p-4" role="dialog" aria-modal="true" aria-label={title}><div className="card w-full max-w-md"><div className="flex justify-between"><h2 className="text-xl font-bold">{title}</h2><button aria-label="Close" onClick={()=>setOpen(false)}>✕</button></div><div className="mt-4">{children}</div></div></div>}</>}
