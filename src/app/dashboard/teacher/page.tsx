import{container}from"@/shared/lib/container";import{createInvite,removeStudent,revokeInvite}from"@/app/actions";import{DashboardShell,Stat}from"@/shared/components/dashboard-shell";
export default async function TeacherDashboard(){const[students,invites]=await Promise.all([container.students().list(),container.invitations().list()]);return<DashboardShell title="Teacher dashboard"><section className="grid gap-4 sm:grid-cols-3"><Stat label="Total students" value={students.length}/><Stat label="Pending invitations" value={invites.filter(x=>x.status==="pending").length}/><Stat label="Accepted invitations" value={invites.filter(x=>x.status==="accepted").length}/></section><section className="card mt-6"><h2 className="text-xl font-bold">Invite a student</h2><form action={createInvite} className="mt-4 flex flex-col gap-3 sm:flex-row"><input className="input" name="email" required type="email" placeholder="student@example.com"/><button className="btn whitespace-nowrap">Send invitation</button></form></section><section className="card mt-6 overflow-x-auto"><h2 className="mb-4 text-xl font-bold">Students</h2><table><thead><tr><th>Email</th><th>Joined</th><th>Action</th></tr></thead><tbody>{students.map(s=><tr key={s.id}><td>{s.profile?.email}</td><td>{new Date(s.created_at).toLocaleDateString()}</td><td><form action={removeStudent}><input type="hidden" name="id" value={s.id}/><button className="btn btn-danger">Remove</button></form></td></tr>)}{!students.length&&<tr><td colSpan={3} className="muted">No students yet.</td></tr>}</tbody></table></section><section className="card mt-6 overflow-x-auto"><h2 className="mb-4 text-xl font-bold">Invitation history</h2><table><thead><tr><th>Email</th><th>Status</th><th>Expires</th><th>Action</th></tr></thead><tbody>{invites.map(i=><tr key={i.id}><td>{i.student_email}</td><td className="capitalize">{i.status}</td><td>{new Date(i.expires_at).toLocaleDateString()}</td><td>{i.status==="pending"&&<form action={revokeInvite}><input type="hidden" name="id" value={i.id}/><button className="btn btn-danger">Revoke</button></form>}</td></tr>)}</tbody></table></section></DashboardShell>}
