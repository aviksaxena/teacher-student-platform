import{requestMagicLink}from"@/app/actions";export default async function Login({searchParams}:{searchParams:Promise<{role?:string}>}){const{role}=await searchParams;const selected=role==="student"?"student":"teacher";return<main className="mx-auto max-w-md px-4 py-20"><div className="card"><h1 className="text-3xl font-bold">Welcome back</h1><p className="muted mt-2">We’ll email you a secure magic link. No password needed.</p><form action={requestMagicLink} className="mt-8 space-y-4"><label className="block font-bold">Email address<input name="email" type="email" required autoComplete="email" className="input mt-2" placeholder="you@example.com"/></label><label className="block font-bold">I am a<select name="role" defaultValue={selected} className="input mt-2"><option value="teacher">Teacher</option><option value="student">Student</option></select></label><button className="btn w-full">Send magic link</button></form></div></main>}
